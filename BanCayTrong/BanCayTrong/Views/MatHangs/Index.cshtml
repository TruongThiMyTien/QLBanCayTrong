@model IEnumerable<BanCayTrong.Models.MatHang>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-info">Danh sách mặt hàng</h1>

<p>
    <a asp-action="Create">Tạo mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Giagoc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Giaban)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soluong)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hinhanh)
            </th>
            <th>
                Danh mục
            </th>
            <th>
               Đã xóa
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Giagoc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Giaban)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Soluong)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mota)
            </td>
            <td>
                <img src="~/images/mathang/@item.Hinhanh" width="50" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MadmNavigation.Ten)
            </td>
            @if (item.Daxoa == 0)
            {
                <td></td>
            }
            else
            {
                <td><i class="bi bi-x-circle-fill text-danger"></i></td>
            }
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mamh">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.Mamh">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.Mamh">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
